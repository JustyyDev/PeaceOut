// Deployment-specific configuration for PeaceOut
// This file can be customized per deployment to override default settings

// Set this to your backend deployment URL when deploying frontend separately
// Uncomment and modify the line below to point to your backend deployment:

// window.PEACEOUT_BACKEND_URL = 'https://your-backend-deployment.vercel.app/api/';
// window.PEACEOUT_BACKEND_URL = 'https://your-backend-deployment.railway.app/api/';
// window.PEACEOUT_BACKEND_URL = 'https://your-backend-deployment.herokuapp.com/api/';

// For production deployment, uncomment one of the following examples:

// Example for Vercel deployment:
// window.PEACEOUT_BACKEND_URL = 'https://peaceout-backend.vercel.app/api/';

// Example for Railway deployment:
// window.PEACEOUT_BACKEND_URL = 'https://peaceout-backend.railway.app/api/';

// Example for custom domain:
// window.PEACEOUT_BACKEND_URL = 'https://api.peaceout.com/api/';

// Example for self-hosted:
// window.PEACEOUT_BACKEND_URL = 'https://your-server.com:4000/api/';