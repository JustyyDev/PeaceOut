import React from 'react'

const CorruptedOverlay: React.FC = () => (
  <div className="corrupted-overlay">
    <h1>Thanks for playing PeaceOut</h1>
    <p>👁️ The stream never ends.</p>
    {/* Add fake ending buttons and effects here */}
  </div>
)

export default CorruptedOverlay