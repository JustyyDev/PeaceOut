// Placeholder for future YouTube-like interactivity
// You can add event listeners and dynamic features here

document.addEventListener('DOMContentLoaded', () => {
  // Example: Focus search bar on load
  const search = document.querySelector('.yt-search');
  if (search) search.focus();
});
